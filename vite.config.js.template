import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://YOUR_API_SERVER:PORT', // Zastąp swoim adresem API
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      },
      '/socket.io': {
        target: 'YOUR_WS_SERVER:PORT', // Zastąp swoim adresem WS
        changeOrigin: true,
        ws: true
      }
    }
  }
})
